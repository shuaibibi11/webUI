## 问题分析

1. **用户13082登录页面错误**：admin-api服务未运行，导致前端无法调用后端API
2. **用户13084服务不可用**：13084端口没有服务在运行
3. **Linting错误**：需要检查并修复代码中的linting问题

## 修复计划

### 1. 启动admin-api服务
   - 运行命令：`cd /data/webUI-java/admin-api && mvn spring-boot:run`
   - 验证服务是否在11025端口正常运行

### 2. 检查并修复13084端口服务
   - 查看前端项目配置，确认13084端口的服务配置
   - 启动或重新配置该服务

### 3. 运行Linting检查并修复错误
   - 运行命令：`cd /data/webUI-java && mvn checkstyle:check` 或相关linting命令
   - 根据输出修复所有linting错误

### 4. 验证修复效果
   - 检查所有服务是否正常运行
   - 测试用户13082的登录页面
   - 测试用户13084的页面访问
   - 再次运行linting检查确认无错误

## 预期结果

- 所有服务正常运行
- 13082用户登录页面无错误
- 13084用户页面显示正常
- 所有linting错误修复完成

## 优先级

1. 启动admin-api服务（最高优先级，影响登录功能）
2. 检查13084端口服务
3. 修复linting错误
4. 验证修复效果