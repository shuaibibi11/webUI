## 修复计划

### 问题分析
1. **admin-api服务**：目前正在运行（端口11025），能够正常处理请求，但之前有端口占用的启动错误
2. **user-api服务**：多个实例正在运行，可能存在端口冲突
3. **依赖问题**：从pom.xml配置来看，admin-api的依赖是完整的，服务能够成功启动和运行，没有明显的依赖缺失
4. **端口占用**：端口11025已被占用，需要清理所有占用的端口后重启服务

### 修复步骤

1. **停止所有运行中的服务**：
   - 停止admin-api服务（端口11025）
   - 停止所有user-api服务实例
   - 停止admin-web服务

2. **清理占用的端口**：
   - 确认所有相关端口（11025, 11026, 13085, 13086）已释放
   - 使用`kill -9`命令强制停止占用端口的进程

3. **重新启动服务**：
   - 先启动admin-api服务，验证启动是否成功
   - 再启动user-api服务
   - 最后启动admin-web服务

4. **验证修复结果**：
   - 测试/admin-api服务的/api/admin/users端点
   - 测试用户登录功能
   - 确保前后端服务正常通信

### 预期结果
- 所有服务成功启动，无错误日志
- /api/admin/users端点返回200 OK状态
- 前端能够正常访问后端服务
- 端口占用问题得到解决

### 注意事项
- 确保服务启动顺序正确：admin-api → user-api → admin-web
- 验证每个服务启动成功后再启动下一个
- 记录启动日志，以便排查可能的问题